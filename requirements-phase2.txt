# Phase 2 Voice Dependencies
# Install only if PHASE_2_ENABLED=true

# ASR (Automatic Speech Recognition)
openai-whisper==20231117
torchaudio==2.1.0

# Audio Processing
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
numpy>=1.24.0

# TTS (Text-to-Speech)
gTTS==2.4.0

# Live Audio Capture
pyaudio==0.2.14

# Voice Activity Detection
webrtcvad==2.0.10

# Optional: Voice Fraud Detection
# Uncomment if VOICE_FRAUD_DETECTION=true
# resemblyzer==0.1.1
# torch>=2.0.0

# Note: PyAudio may require system dependencies:
# - Windows: pip install pipwin && pipwin install pyaudio
# - Linux: sudo apt-get install portaudio19-dev python3-pyaudio
# - Mac: brew install portaudio && pip install pyaudio
